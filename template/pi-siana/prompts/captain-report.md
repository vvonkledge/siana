---
description: Write the captain's fleet report from the live stores, with every decision they have to take
argument-hint: "[project handle]"
---
Write the captain's fleet report now, for ${1:-every project in the registry}.

Load the `captain-report` skill and follow it exactly. It is the procedure; this
command is only the way the captain asks for it, so nothing about how the report is
written lives here. Start with `siana-report --json`, and write nothing from
recollection.
